# Pokedex Feature

## 역할
- 기존 포켓몬 도감 화면과 프론트 로직을 담당합니다.
- 다른 팀원이 기능을 추가할 때 이 폴더를 복사해서 새 기능 폴더를 만들 수 있습니다.

## 포함 파일
- `backend.py`: 기능별 Flask 블루프린트
- `frontend.js`: 기능 전용 프론트 로직
- `frontend.css`: 기능 전용 스타일
- `template_fragment.html`: 메인 템플릿에 삽입되는 HTML 조각
